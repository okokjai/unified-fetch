# Tier 0 — installed and ready, zero config
# browser/ core hard dependencies are only mcp + websockets (rest CDP all stdlib)

mcp>=1.0.0
websockets>=12.0

# HTTP layer (L0-L1)
httpx>=0.27.0

# Search (Tier 0)
duckduckgo_search>=6.0.0

# Content extraction (Tier 0)
trafilatura>=1.8.0
readability-lxml>=0.8.1
lxml[html_clean]>=5.2.0
justext>=3.0.0
newspaper3k>=0.2.8
