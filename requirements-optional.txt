# Tier 1 — 選配引擎，裝了自動啟用，不裝自動跳過

# Hound（master-fetch）：深度反爬 / PDF OCR / neural ranking
# https://github.com/dondai1234/master-fetch
master-fetch

# Playwright：僅 browser_interact 的 drag + frame 動作 fallback
# 瀏覽器二進位需另裝：python -m playwright install chromium
playwright>=1.45.0

# L2 TLS 指紋（保留介面，目前未強制使用）
curl_cffi>=0.7.0

# 備用搜索
googlesearch-python>=1.2.0
